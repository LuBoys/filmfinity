<header class="header">
    <div class="container">
        <div class="row">
            <div class="col-lg-2">
                <div class="header__logo">
                    <a href="{{ path('app_homepage') }}">
                        <img src="{{ asset('assets/img/logo3.png') }}" alt="">
                    </a>
                </div>
            </div>
            <div class="col-lg-8">
                <div class="header__nav">
                    <nav class="header__menu mobile-menu">
                        <ul>
                            <li class="active"><a href="{{ path('app_homepage') }}">Accueil</a></li>
                            <li>
                                <a href="./categories.html">Categories <span class="arrow_carrot-down"></span></a>
                                <ul class="dropdown"></ul>
                            </li>
                            <li><a href="./categories.html">Film</a></li>
                            <li><a href="./anime-watching.html">Les plus populaires</a></li>
                            <li><a href="./blog-details.html">Dernière Sortie</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li><a href="{{ path('admin') }}">CRUD</a></li>
                            {% endif %}
                        </ul>
                    </nav>
                </div>
            </div>
            <div class="col-lg-2">
                <div class="header__right">
                    <a class="search-switch"><span class="icon_search"></span></a>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a href="{{ path('app_profile') }}" style="color: white;">{{ app.user.name }}</a>
                    {% else %}
                        <a href="{{ path('app_login') }}" style="color: white;"><span class="icon_profile"></span></a>
                    {% endif %}
                </div>
            </div>
        </div>
        <div id="mobile-menu-wrap"></div>
    </div>
    <!--fin header-->
</header>